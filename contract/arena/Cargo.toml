[package]
name = "arena"
version = "0.1.0"
edition = "2024"

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
proptest = "1"
serde_json = "1"
factory = { path = "../factory" }
payout = { path = "../payout" }
